# railway.toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python agent.py"
cronSchedule = "0 8 * * *"   # runs every day at 8:00 AM UTC (adjust to your timezone)
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
