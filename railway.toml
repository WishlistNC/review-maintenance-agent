# railway.toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python agent.py"
cronSchedule = "0 12 * * *"   # runs every day at 8:00 AM EST (adjust to your timezone)
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
